<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".BudgetManagerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BudgetManager"
        tools:targetApi="31">

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.BudgetManager.Auth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:theme="@style/Theme.BudgetManager.Auth" />

        <activity
            android:name=".ui.auth.ForgotPasswordActivity"
            android:theme="@style/Theme.BudgetManager.Auth" />

        <activity
            android:name=".ui.auth.ChangePasswordActivity"
            android:theme="@style/Theme.BudgetManager.Auth" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.BudgetManager" />

        <activity
            android:name=".ui.category.AddEditCategoryActivity"
            android:theme="@style/Theme.BudgetManager" />

        <activity
            android:name=".ui.article.AddEditArticleActivity"
            android:theme="@style/Theme.BudgetManager" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
